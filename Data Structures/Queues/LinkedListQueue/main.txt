#include <iostream>
#include "Queue.h"

using namespace std;

int main()
{
    cout << boolalpha; // to print bool as true and false instead of 1 and 0
    Queue q1;

    cout << q1;

    q1.enqueue(6);
    q1.enqueue(10);
    q1.enqueue(15);

    cout << q1;

    q1.dequeue();

    cout << q1;

    cout << q1.getFront() << endl;

    q1.dequeue();
    cout << q1;

    q1.dequeue();
    cout << q1;

    for (int i = 0; i < 10; i++)
    {
        q1.enqueue(i);
    }

    cout << q1;

    Queue q2;
    q2 = q1;
    cout << q2;

    for (int i = 0; i < 4; i++)
    {
        q1.dequeue();
    }
    cout << q1.getFront() << endl;

    cout << "q1: " << q1 << "q2: " << q2 << endl;

    q1 = q2;

    cout << "q1: " << q1 << "q2: " << q2 << endl;

    Queue q3(q1);

    for (int i = 0; i < 12; i++)
    {
        cout << q3.dequeue();
    }

    cout << q3;
}
