/*
In a Binary Tree, you are given the a pointer to the root of the tree, and an
integer k If there are 2 paths such that the sum of each path is equal to k,
returns both paths in a vector.
Otherwise, return {-1, -1}

Note: When writing the path, negative numbers are not included

A path is written as an int with the digits you must traverse, listed from
left to right

Ex: path  5327  means  5 --> 3 --> 2 --> 7 (there could be a negative number
between the numbers)

*/

#include <bits/stdc++.h>
#include <vector>

using namespace std;

vector<int> getPaths(TreeNode *root, int target) {}